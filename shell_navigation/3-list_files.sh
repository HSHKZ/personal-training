#!/bin/bash

# order : Write a script that lists all files in the current directory.
# consigne : Écrivez un script qui répertorie tous les fichiers du répertoire actuel.
# commande : ls (list / lister) + option -l (long format) 

ls -la
