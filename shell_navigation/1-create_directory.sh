#!/bin/bash

# order : Write a script that creates a directory named my_directory.
# consigne : Écrivez un script qui crée un répertoire nommé my_directory.
# commande : mkdir (make directory / creer un repertoire)

mkdir my_directory
